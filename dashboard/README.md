# dashboard/ — Next.js (Phase 5 placeholder)

Intentionally empty in Phase 0. The read-mostly monitoring dashboard (live watchlist,
positions, `risk_events` log, day P&L vs guard thresholds, kill-switch) is built in **Phase 5**
per `ROSSBOT_PROJECT_PLAN.md`. No mid-session parameter editing (mirrors U11).

Next.js/React versions will be web-verified at the start of Phase 5, not now.
